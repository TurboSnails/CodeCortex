# Tasks

- [ ] 1. 编写 `touzi.py`：实现 `StrategySimulator`、`borrow_2x`、`leverage_etf` 模型、触发器参数、反弹曲线计算
- [ ] 2. 编写 `touzi_test.py`：覆盖关键档位 + NAV 边界
- [ ] 3. 运行 `python touzi.py` 输出两种模型对比表 + 用户原题场景的累计盈亏
- [ ] 4. 验证两者数值正确性后，结论返回给用户
